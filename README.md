# Discord-Bot
A discord bot

## Running

To run go version:
```bash
cd src
go run main.go
```

To run elixir version:
```bash
cd elixir_bot
eix -S mix
```

## Default prefix

Go version is ``:>``

Elixir version is ``<>``
